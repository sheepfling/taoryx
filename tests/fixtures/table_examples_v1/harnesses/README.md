# Table Example Problem Harnesses

This directory contains small `.prb` harnesses that reference the example
table families under `tests/fixtures/table_examples_v1/`.

The harnesses are intentionally simple:

- they stay close to parser and ingestion coverage;
- they reference the table families directly instead of building a full
  simulation corpus;
- they are meant to expand incrementally as new table families are added.

