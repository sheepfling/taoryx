# Family-specific verification views

New family tests belong under `tests/families/<family-id>/` and should use the
common onboarding, convention, trim, closure, convergence, session, and
objective utilities. Do not create a family-specific runner when a common
harness can express the case.
