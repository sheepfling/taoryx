# Developer template: raw values are simulation assumptions, not source facts.
interceptor_id: generic-medium-sam-example
variant: baseline
parameter_set_version: 0.1.0
surrogate_archetype_id: generic_slender_sam_v1

launch_mass_kg: 420.0
propellant_fraction: 0.42
length_m: 5.2
body_diameter_m: 0.36
propulsion_architecture: single_stage
burn_time_class: medium
thrust_profile_class: boost_sustain
# Optional simulation-only calibration scales. Omit to use visible 1.0 defaults:
# thrust_scale: 1.0
# drag_scale: 1.0
# maneuverability_scale: 1.0
# guidance_time_constant_scale: 1.0
# Optional exact single-pulse timing:
# burn_time_s: 8.0
# For dual_pulse_solid, replace it with the explicit pulse program below:
# first_pulse_burn_time_s: 3.5
# inter_pulse_coast_time_s: 4.0
# second_pulse_burn_time_s: 4.5
# second_pulse_thrust_ratio: 0.8
# second_pulse_propellant_fraction: 0.5
aero_archetype: generic_slender_supersonic
drag_class: nominal
maneuverability_class: high
# Optional explicit force-authority override. Omit these to use labeled
# archetype defaults selected from maneuverability_class.
# control_configuration: mixed  # aerodynamic | thrust_assisted | mixed
# control_allocation_policy: aerodynamic_first  # aerodynamic_first | thrust_vector_first | proportional
# normal_force_coefficient_limit: 8.0
# max_thrust_vector_angle_rad: 0.1745329252  # 10 deg
# maneuver_drag_factor: 0.1  # Cdm = factor * achieved_Cn^2
control_bandwidth_class: fast
guidance_family: command
guidance_archetype: waypoint_pursuit
# proportional_navigation uses this dimensionless gain; pursuit still advertises it for schema stability.
# navigation_constant: 3.0
# Optional advisory model domain. No bounds are fabricated when omitted.
# applicability_altitude_min_m: 100.0
# applicability_altitude_max_m: 25000.0
# applicability_mach_min: 0.1
# applicability_mach_max: 5.0
# Optional advanced response-law overrides use the same flat profile shape:
# attitude_bandwidth_rad_s: 8.5
# attitude_damping_ratio: 0.9
# max_body_rate_rad_s: 3.5
# max_body_acceleration_rad_s2: 16.0
# max_bank_angle_rad: 1.3
reported_max_speed_mps: 1200.0
reported_max_range_m: 45000.0
reported_max_altitude_m: 24000.0
target_classes:
  - air_breathing
  - cruise_missile

model_notes: >-
  Replace raw values with inline evidence records as ontology sources become
  available. Reported performance values are calibration targets, not direct
  force-model parameters.
