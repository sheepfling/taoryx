# TAOS 96 syntax fragments

The `chapter03` link is the canonical historical table-fixture directory.
Use it for small parser experiments and table-language changes. Do not add
successor syntax here; place that material under `examples/taoryx/syntax_fragments/`.
